import torch

class DQN(object):
    def __init__(self, config, env, device):
        self.config = config
        self.env = env
        self.device = device

    def training_step(self, state):



    def fit_model(self):
        pass